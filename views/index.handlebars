<h1>Eat some sushi!!</h1>

<h2>Gimme that sushi!</h2>

<ul>
  {{#each sushi}}
    {{#unless devoured}}
      {{> sushi/sushi-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>That sushi was delicious!</h2>

<ul>
  {{#each sushi}}
    {{#if devoured}}
      {{> sushi/sushi-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add some Sushi</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">What kind of sushi?:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="slee">Hungry?</label><br>
    <input type="radio" name="devoured" value="1" checked> That was delicious!<br>
    <input type="radio" name="devoured" value="0"> Eat it up
  </div>

  <button type="submit">Add Sushi</button>
</form>